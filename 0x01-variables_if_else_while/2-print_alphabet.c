#include <stdio.h>

/**
 *main - receives no argument
 *
 *Return: Always 0 (success)
 */

int main(void)
{
	char aplha;

	for(alpha = "a"; alpha <= "z"; alpha++)
	{
		printf("%c\n", alpha);
	}

	return (0);
}
